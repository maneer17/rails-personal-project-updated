- content_for :title, t(".title")
%h1 Editing submission
= render "form", submission: @submission
%br/
%div
  = link_to t(".show_submission"), @submission
  |
  \#{link_to "Back to submissions", submissions_path}
